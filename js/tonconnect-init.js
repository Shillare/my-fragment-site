// TonConnect + Auction integration for Fragment Mini App
// Uses esm.sh CDN for @tonconnect/sdk and @ton/core
// Import happens via <script type="module"> in the HTML

// The actual module code is injected inline in the template
// to avoid CORS issues with CDN imports on static hosting.
// This file serves as documentation and the source of truth.
//
// The code is embedded in template_original.html near the
// end of the <body> as a <script type="module"> block.
